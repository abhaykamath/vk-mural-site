# Needed to build. The site reads the CMS database once, at build time, and
# ships plain HTML — nothing here reaches a visitor's browser.
#
# This key is limited by row level security to reading published rows and cannot
# write anything, which is why it is safe in a build environment.
SUPABASE_URL=https://vbsbcbelqruiiggwfrwl.supabase.co
SUPABASE_ANON_KEY=
